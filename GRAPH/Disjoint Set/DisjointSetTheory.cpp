/*
  * Disjoint set DS allows us to check if two nodes in a graph 
  ! & BELONG to the SAME COMPONENT in O(1) Constand TIME
  * DISJOINT SET Use CASE:
  * Dynamic Graphs (Graphs which keep on changing the configuration every moment)
  * Functions() :
  * findParent()
  * unionOfGraph() - can be found in 2 ways: Rank and Size
*/